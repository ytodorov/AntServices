@{
    ViewBag.Title = "JSON tools";
    ViewBag.MetaDescription = "JSON minifier and beautifier.";
}
<script src="~/Scripts/css/index.js"></script>
<div class="normalheader transition animated fadeIn small-header">
    <div class="hpanel">
        <div class="panel-body">
            <div id="hbreadcrumb" class="pull-right">
                <ol class="hbreadcrumb breadcrumb">
                    <li>
                        <a href="/">Home</a>
                    </li>
                    <li>
                        <a href="/css">JSON processing</a>
                    </li>
                </ol>
            </div>
            <h2 class="font-light m-b-xs">
                JSON processing
            </h2>
            <small>Minify and beautify JSON.</small>
        </div>
    </div>
</div>

@using (Html.BeginForm("Download_Document", "json"))
{
    @(@Html.Raw(Html.AntCreateStandardPanel("hgreen", "Upload files (.json)",
    Html.Kendo().Upload()
                        .Name("jsonUploads")
                        .Multiple(true)
                        .ShowFileList(true)
                        .DropZone("#wrapper")
                        .Validation(v => v.AllowedExtensions(new string[] { "json" }).MaxFileSize(10 * 1024 * 1024))
                        .Deferred().ToHtmlString()
    )))

    @(@Html.Raw(Html.AntCreateStandardPanel("hgreen", "Minified files", @"
    <p class='text-center'>
        <input type = 'submit' class='k-button k-primary wide-btn' value='Minify and Download' />
    </p>")))

}
